# Git 属性配置
#
# 本仓库同时要在 macOS / Linux / Windows 三平台构建（见 .github/workflows/release.yml），
# 因此这里主要解决三件事：行尾一致、二进制不被转换、以及 GitHub 的语言占比正确
# ——仓库里带着 4 MB 的 `wails init` 脚手架，不显式排除就会盖过自写源码。

# ── 行尾：仓库内统一存 LF，检出也一律 LF ─────────────────────
# 若放任行尾随平台漂移，换一台机器提交就会把每个文件重写成「整文件变更」，
# 真正的改动被淹没在几万行 diff 里。
* text=auto eol=lf

# Windows 批处理必须 CRLF：cmd.exe 对 LF 结尾的批处理解析不可靠
# （gradlew.bat 是 Android 子工程的一部分，由 Gradle 生成）
*.bat text eol=crlf
*.cmd text eol=crlf

# ── 脚本：确保 LF ───────────────────────────────────────────
# shell 脚本若带 CRLF，shebang 会变成 `#!/bin/sh\r`，
# 在 Linux/macOS 上直接报 "bad interpreter" 而无法执行。
# `*` 规则已覆盖，这里显式写出以表明这是有意为之，而非默认继承。
*.sh   text eol=lf
gradlew text eol=lf
*.desktop text eol=lf

# ── 二进制：禁止任何行尾或字符集转换 ─────────────────────────
# 这些文件若被当作文本处理，会被损坏且 git 无法还原。
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.icns  binary
*.ttf   binary
*.otf   binary
*.woff  binary
*.woff2 binary
*.jar   binary
*.car   binary

# ── 上游脚手架 / 生成物：排除语言统计、折叠 diff ────────────
# 这一节同时修「GitHub 语言占比」。修复前 api.github.com/repos/.../languages
# 返回 Go 131833 / Java 100220 / TypeScript 99589 / NSIS 13659 —— 其中一大块
# 是 `wails init` 的脚手架和自动生成代码，不是本项目写的：
#
#   · Java 100,220 B  = build/android 下 Wails 的原生壳（WailsBridge、MainActivity）
#   · NSIS  13,659 B  = build/windows/nsis 的安装器模板
#   · Go   里的 20,191 B = build/{ios,android} 的平台入口 + 依赖安装脚本
#     （131,833 = internal/ 109,704 + main.go 1,938 + build/ 20,191，可逐字节对上）
#
# 这些文件由 8a85ff4「将 build/ 构建脚手架纳入版本控制」整体入库，主体来自
# wails init，此后只有零星本地修补（如 103b821），仍应算上游代码。
#
# 用 linguist-vendored 而不是 linguist-generated：两者在语言统计上完全等价，
# 但 generated 会额外把文件在 diff 中折叠隐藏，而 build/ 下的脚手架值得在
# review 里正常展开——它只是不该计入语言占比。vendored 恰好只做前者。
#
# 改完后的分布：Go ≈ 111,642 B（internal/ + main.go），TypeScript ≈ 99,627 B
# （frontend/src + vite.config.ts），两者正好等于本仓库真实的两部分源码，
# Java / NSIS 归零。
build/**                     linguist-vendored

# frontend/bindings 由 Wails 依据 Go 结构体自动生成，文件头即标注
# "This file is automatically generated. DO NOT EDIT"，不应手改，也不应在
# review 里逐行展开。
frontend/bindings/**         linguist-generated=true

# 嵌套的 npm 包，同理。
#
# 实测结论：bindings 与 go.sum 在本次改动前就已经不计入统计了。GitHub 报的
# TypeScript 99,589 / Go 131,833 与「frontend/src + vite.config.ts」和
# 「internal/ + main.go + build/ 下的 .go」逐字节吻合，bindings(20,232 B)、
# go.sum(8,545 B) 都已被排除。真正被算错的只有 build/ 一项。
#
# 保留下面两行是防御性的：GitHub 的语言统计不是每次 push 都同步，而这两个
# 属性位于各自目录内无其他规则的裸路径下、只作用于这两个文件本身，即使哪次
# 统计口径变了也能保持正确。
frontend/package-lock.json   linguist-generated=true

# go.sum 是模块哈希清单，机器生成。注意 .sum 并非 Linguist 已知扩展名，
# 一旦此处失效，它会退化到按内容猜测语言（go.sum 曾出现被判成 OpenEdge ABL
# 的 issue），故保留显式声明。
go.sum                       linguist-generated=true

# 注：NSIS 用到的 MicrosoftEdgeWebview2Setup.exe 已被 .gitignore 排除、
# 不在版本库内，故此处不需要（也无法）为它声明属性。

# ── 文本但容易被误判的格式 ──────────────────────────────────
*.pbxproj    text eol=lf
*.storyboard text eol=lf
